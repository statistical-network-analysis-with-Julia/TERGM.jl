# Estimation and Simulation

```@meta
CurrentModule = TERGM
```

`fit_stergm` is an exported alias for [`stergm`](@ref).

```@docs
stergm
cmple
cmle
egmme
simulate_stergm
simulate_network_sequence
gof(::STERGMResult)
stergm_gof
```
